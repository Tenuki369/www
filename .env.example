# Anthropic API key — required to run agents in the Multi-Agent Workflow Studio.
# Create one at https://console.anthropic.com/ and copy it into .env.local.
ANTHROPIC_API_KEY=

# Postgres connection string for Prisma (not required for the demo session or
# the agent studio; needed once real persistence is wired up).
DATABASE_URL=
